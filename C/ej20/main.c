#include <stdio.h>
#include <stdlib.h>

int main()
{
        int num,cont,bandera=1,factorial;
        printf("ingrese num");
        scanf("%d",&num);
        cont=num;
        while (cont>0)
        {
            cont--;
            factorial=num*cont;

        }


        return 0;
}
