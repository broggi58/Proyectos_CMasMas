#include <stdio.h>
#include <stdlib.h>

int main()
{int a,max,flag=0;
char n[20];
printf("altura");
fflush(stdin);
scanf("%d",&a);
printf("nombre");
fflush(stdin);
scanf("%s",n);
if (flag==0)
{
    max=a;
    strcpy(n,max);
}
    printf("Hello world!\n&d &s",a,n);
    return 0;
}
