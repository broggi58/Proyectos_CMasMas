#include <stdio.h>
#include <stdlib.h>

int main()
{
        int num=6d;
        int suma=6;
        while (suma<=100)
        {
            printf("%d",suma);
            suma+=num;
        }

        return 0;
}
